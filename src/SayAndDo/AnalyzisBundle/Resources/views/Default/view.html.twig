{% extends 'base.html.twig' %}

{% block content %}
    <div class="row">
        <h1>{{ document.title }}
            <small><a href="{{ document.url }}" target="_blank"><i class="fa fa-external-link"></i></a></small>
        </h1>
        <p>
            {{ document.content }}
        </p>
    </div>
{% endblock %}
